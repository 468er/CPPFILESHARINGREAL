#include "Ingredient.h"
#include <list>

#include <vector>
int main(){
   std::vector<int> test;
   test.push_back(0);
      test.push_back(1);
   test.push_back(2);
   std::vector<int>::iterator AnotherTest = test.begin();
   }